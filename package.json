{
  "name": "brasauto-compras",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && prisma db execute --schema prisma/schema.prisma --file prisma/migrations/0003_integrity_guards/migration.sql && next build",
    "build:setup": "prisma generate && prisma db push && prisma db seed && next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:validate": "prisma validate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "tsx prisma/seed.ts",
    "db:repair-finance": "tsx prisma/repair-finance.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "prisma": "^6.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
